import { useEffect, useState } from 'react';

const Login = () => {
  const [data, setData] = useState(null);

  return (
    <div className="">
      <h1>Login</h1>
    </div>
  );
}

export default Login;
